const {expect} = require('chai');
describe('tic-tac-toe', () => {
  it ('work when you test it', () => {
    expect(true).to.equal(true);
  });
  // board correct
  // next move row, next move column
  // moves
  // rowchange
  // colchange
  // move already selected
  // X or O
  // >9 moves
  // victory
  // tie
});